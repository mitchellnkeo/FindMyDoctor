{
  "name": "findmydoctor",
  "version": "1.0.0",
  "description": "An app for users to find a healthcare provider most suitable for their needs.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "psql -f migration.sql findMyDoctor; psql -f seed.sql findMyDoctor"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
